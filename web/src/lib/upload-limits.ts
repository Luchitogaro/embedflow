/** Max file size for contract uploads (PDF/DOCX/TXT). Must stay below Next.js proxy + serverActions body limits. */
export const UPLOAD_MAX_FILE_BYTES = 35 * 1024 * 1024
export const UPLOAD_MAX_FILE_MB = 35
